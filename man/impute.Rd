% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{impute}
\alias{impute}
\title{Unified impute() wrapper}
\usage{
impute(
  data,
  time = NULL,
  status = NULL,
  groups = NULL,
  model = c("auto", "parametric", "nonparametric"),
  distribution = c("weibull", "exponential", "lognormal", "nonparametric", "np", "lddp",
    "nonparametric_lddp"),
  n_imputations = 10,
  time_unit = "days",
  priors = NULL,
  prior = NULL,
  mcmc_options = NULL,
  mcmc = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{Data frame or \code{survival_data} prepared by \code{prepare_survival_data()}}

\item{time}{Time column name. Required unless \code{data} is a \code{survival_data} object.}

\item{status}{Status column name (1=event, 0=censored). Required unless \code{data} is a \code{survival_data} object.}

\item{groups}{Optional group variable name for group analyses}

\item{model}{One of c("auto","parametric","nonparametric"). Default "auto".}

\item{distribution}{For parametric: one of c("weibull","exponential","lognormal").
Nonparametric can be requested via distribution tokens in
\code{c("nonparametric","np","lddp","nonparametric_lddp")}.}

\item{n_imputations}{Number of completed datasets to generate}

\item{time_unit}{Display unit for times (e.g., "days","months","years")}

\item{priors}{Parametric priors list (see \code{get_default_priors()} / \code{get_adaptive_priors()})}

\item{prior}{Nonparametric prior list (LDDP hyperparameters)}

\item{mcmc_options}{Parametric MCMC options list}

\item{mcmc}{Nonparametric MCMC options list}

\item{verbose}{Logical; print progress}

\item{...}{Passed through to the underlying engine}
}
\value{
A \code{bayesian_imputation} or \code{bayesian_imputation_groups} object
}
\description{
A single entry point that routes to either parametric (\code{bayesian_impute})
or nonparametric LDDP (\code{bayes_np_impute}) engines based on arguments.
}
\details{
\itemize{
\item If \code{model = "nonparametric"} or \code{distribution} is one of
\code{c("nonparametric","np","lddp","nonparametric_lddp")}, uses the
nonparametric engine.
\item Otherwise, uses the parametric engine with the chosen \code{distribution}.
}
}
\seealso{
\code{\link[=bayesian_impute]{bayesian_impute()}}, \code{\link[=bayes_np_impute]{bayes_np_impute()}}, \code{\link[=complete]{complete()}}
}
